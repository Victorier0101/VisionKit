import { PhasePlaceholder } from "@/components/test/PhasePlaceholder";
export default function Page() {
  return (
    <PhasePlaceholder
      title="Settings"
      phase="Phase 9"
      description="Local data controls will appear alongside result history. VisionKit has no account or remote database."
    />
  );
}
